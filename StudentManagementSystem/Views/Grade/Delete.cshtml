@model StudentManagementSystem.Models.Grade

@{
    ViewData["Title"] = "Delete";
}

<h1>Delete</h1>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Grade</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Enrollment)
        </dt>
        <dd class = "col-sm-10">@Model StudentManagementSystem.Models.Grade

@{
    ViewData["Title"] = "Delete";
}

<h1>Delete</h1>

<h3>Are you sure you want to delete this grade?</h3>
<div>
    <h4>Grade</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            Student - Course
        </dt>
        <dd class="col-sm-10">
            @(Model.Enrollment != null && Model.Enrollment.Student != null && Model.Enrollment.Course != null
                ? $"{Model.Enrollment.Student.StudentName} - {Model.Enrollment.Course.CourseName}"
                : "N/A")
        </dd>

        <dt class="col-sm-2">
            Letter Grade
        </dt>
        <dd class="col-sm-10">
            @Model.LetterGrade
        </dd>
    </dl>

    <form asp-action="Delete">
        <input type="hidden" asp-for="GradeId" />
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        <a asp-action="Index">Back to List</a>
    </form>
</div>
            @Html.DisplayFor(model => model.Enrollment.EnrollmentId)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.LetterGrade)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.LetterGrade)
        </dd>
    </dl>
    
    <form asp-action="Delete">
        <input type="hidden" asp-for="GradeId" />
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        <a asp-action="Index">Back to List</a>
    </form>
</div>
