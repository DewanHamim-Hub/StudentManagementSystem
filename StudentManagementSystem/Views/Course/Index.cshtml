@model IEnumerable<StudentManagementSystem.Models.Course>

@{
    ViewData["Title"] = "Course Management";
}

<div class="container my-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2><i class="fas fa-book me-2"></i>Course Management</h2>
        <a asp-action="Create" class="btn btn-success">
            <i class="fas fa-plus-circle me-1"></i>Create New Course
        </a>
    </div>

    <div class="table-responsive">
        <table class="table table-hover shadow-sm text-center align-middle">
            <thead class="bg-dark text-white">
                <tr>
                    <th scope="col"><i class="fas fa-book-open me-1"></i>Course Name</th>
                    <th scope="col"><i class="fas fa-chalkboard-teacher me-1"></i>Assigned Teacher</th>
                    <th scope="col" style="width: 260px;"><i class="fas fa-tools me-1"></i>Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.CourseName</td>
                        <td>@item.Teacher?.TeacherName</td>
                        <td>
                            <a asp-action="Edit" asp-route-id="@item.CourseId" class="btn btn-sm btn-warning me-1">
                                <i class="fas fa-edit me-1"></i>Edit
                            </a>
                            <a asp-action="Details" asp-route-id="@item.CourseId" class="btn btn-sm btn-info me-1 text-white">
                                <i class="fas fa-info-circle me-1"></i>Details
                            </a>
                            <a asp-action="Delete" asp-route-id="@item.CourseId" class="btn btn-sm btn-danger">
                                <i class="fas fa-trash-alt me-1"></i>Delete
                            </a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>